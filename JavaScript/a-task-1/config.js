const config = {
  db: {
    host: "127.0.0.1",
    port: 5432,
    database: "test",
    user: "test",
    password: "test",
  },
  staticPort: 8000,
  port: 8001,
  transport: "http",
};

module.exports = config;
